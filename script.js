document.querySelector('form').addEventListener('submit', function (event) {
    event.preventDefault();
    alert('Vielen Dank für Ihre Nachricht!');
    // Hier können Sie zusätzliche Logik für das Formular hinzufügen
});